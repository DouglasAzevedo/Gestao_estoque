<%= form_with(model: gprod, local: true) do |form| %>
  <% if gprod.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gprod.errors.count, "error") %> prohibited this gprod from being saved:</h2>

      <ul>
      <% gprod.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
